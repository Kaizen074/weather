window.onload = () => {
    /*const { ipcRenderer } = require('electron');
    ipcRenderer.on('a', (event, arg) => {
        document.getElementById("main").innerHTML += "response";
    });


    document.getElementById("pin").addEventListener("change", event => {
        ipcRenderer.send("window-control", "pin");
    });


    function notification(title, body){
        new Notification(title, {
            body: body,
            icon: '../favicon.ico'
        }).onclick = () => {
            ipcRenderer.send("window-control", "show");
        }
    }

    const NOTIFICATION_TITLE = 'Steroid';
    const NOTIFICATION_BODY = 'Electron reloaded...';
    //notification(NOTIFICATION_TITLE, NOTIFICATION_BODY);
    */
};